<ul>
  <% @cats.each do |cat|%>
  <li>
    <%= link_to cat.name, cat_url(cat)%>
  </li>
 <%end%>
</ul>

<%= link_to "Create a Cat!", new_cat_url %>

<p><%= link_to "Rent a cat!", new_cat_rental_request_url %></p>

<h2>You are currently logged in at:</h2>
<%= request.remote_ip %>